services:
  tg-class-bot:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    env_file:
      - .env
    volumes:
      - ./DATABASE_DO_NOT_DELETE:/app/DATABASE_DO_NOT_DELETE
    restart: unless-stopped
    command: ["uv", "run", "main.py"]
